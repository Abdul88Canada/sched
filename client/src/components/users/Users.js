import { Button } from "react-bootstrap";

const Users = () => {
    return (
        <div>
            <Button variant='falcon-primary' className='me-2 mb-1'>Add User</Button>
        </div>
    )
}

export default Users;